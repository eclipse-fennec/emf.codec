# Frozen spec baseline — 2026-07-24

Snapshot of `docs/codec-v2-spec/` taken before the fingerprinting spec work
(see `docs/codec-v2-fingerprinting-workdoc.md`). This copy is READ-ONLY by
convention: it exists so the pre-fingerprinting state is always at hand without
digging through git history. All changes go into the live spec at
`docs/codec-v2-spec/`.
